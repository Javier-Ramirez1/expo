apiVersion: apps/v1
kind: Deployment

metadata:
  name: pedidos-deployment

spec:
  replicas: 1

  selector:
    matchLabels:
      app: pedidos

  template:
    metadata:
      labels:
        app: pedidos

    spec:
      containers:
        - name: pedidos
          image: pedidos-service:latest
          imagePullPolicy: Never

          ports:
            - containerPort: 8083

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"